# 块状数组
[toc]

## 介绍
### 定义
将长度为 $n$ 的数组分成大小为 $k$ 的块，共有 $m=\lceil \frac{n}{k} \rceil$ 个块
> 最后一个块可能不完整。

### 核心思想
- **大段维护**：维护整块信息或懒标记，复杂度 $O(m)$
- **局部朴素**：块内信息暴力处理，复杂度 $O(k)$

### 复杂度分析
- 整体复杂度 $O(m + k)$
- $k=m=\sqrt{n}$ 时复杂度最低，即 $O(\sqrt{n})$
> 根据均值不等式: $k + m \geq 2\sqrt{km}$

### 应用
几乎所有区间查询问题，甚至线段树不能处理的问题
- 区间和
- 区间最大值
- 区间第k小
- ......

## 图示
<svg viewBox="0 0 640 195" width="100%" height="100%" xmlns="http://www.w3.org/2000/svg">
    <defs>
        <style>
            .cell { stroke: #555; stroke-width: 1.5; }
            .b0 { fill: #d6eaf8; }
            .b1 { fill: #d5f5e3; }
            .b2 { fill: #fcf3cf; }
            .b3 { fill: #fadbd8; }
            .idx { font-family: monospace; font-size: 13px; text-anchor: middle; fill: #333; }
            .blk-label { font-family: sans-serif; font-size: 11px; font-weight: bold; fill: #555; text-anchor: middle; }
            .block-frame { fill: none; stroke: #e67e22; stroke-width: 2; stroke-dasharray: 6,4; }
            .query-line { stroke: #c0392b; stroke-width: 3; fill: none; marker-start: url(#arrowL); marker-end: url(#arrowR); }
            .annotation { font-family: sans-serif; font-size: 12px; fill: #c0392b; text-anchor: middle; font-weight: bold; }
            .complexity { font-family: sans-serif; font-size: 11px; font-weight: bold; fill: #e67e22; text-anchor: middle; }
            .bracket { stroke: #333; stroke-width: 2.5; fill: none; }
        </style>
        <marker id="arrowL" markerWidth="8" markerHeight="6" refX="0" refY="3" orient="auto">
            <polygon points="8 0, 0 3, 8 6" fill="#c0392b" />
        </marker>
        <marker id="arrowR" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto">
            <polygon points="0 0, 8 3, 0 6" fill="#c0392b" />
        </marker>
    </defs>
    <!-- 块0：下标 0-3 -->
    <rect class="cell b0" x="20" y="80" width="40" height="35" /><text class="idx" x="40" y="102">0</text>
    <rect class="cell b0" x="60" y="80" width="40" height="35" /><text class="idx" x="80" y="102">1</text>
    <rect class="cell b0" x="100" y="80" width="40" height="35" /><text class="idx" x="120" y="102">2</text>
    <rect class="cell b0" x="140" y="80" width="40" height="35" /><text class="idx" x="160" y="102">3</text>
    <!-- 块1：下标 4-7 -->
    <rect class="cell b1" x="180" y="80" width="40" height="35" /><text class="idx" x="200" y="102">4</text>
    <rect class="cell b1" x="220" y="80" width="40" height="35" /><text class="idx" x="240" y="102">5</text>
    <rect class="cell b1" x="260" y="80" width="40" height="35" /><text class="idx" x="280" y="102">6</text>
    <rect class="cell b1" x="300" y="80" width="40" height="35" /><text class="idx" x="320" y="102">7</text>
    <!-- 块2：下标 8-11 -->
    <rect class="cell b2" x="340" y="80" width="40" height="35" /><text class="idx" x="360" y="102">8</text>
    <rect class="cell b2" x="380" y="80" width="40" height="35" /><text class="idx" x="400" y="102">9</text>
    <rect class="cell b2" x="420" y="80" width="40" height="35" /><text class="idx" x="440" y="102">10</text>
    <rect class="cell b2" x="460" y="80" width="40" height="35" /><text class="idx" x="480" y="102">11</text>
    <!-- 块3（不完整）：下标 12-14 -->
    <rect class="cell b3" x="500" y="80" width="40" height="35" /><text class="idx" x="520" y="102">12</text>
    <rect class="cell b3" x="540" y="80" width="40" height="35" /><text class="idx" x="560" y="102">13</text>
    <rect class="cell b3" x="580" y="80" width="40" height="35" /><text class="idx" x="600" y="102">14</text>
    <!-- 虚线框：块1 只框下标7 -->
    <rect class="block-frame" x="300" y="75" width="40" height="45" />
    <!-- 虚线框：块2 整块 8-11 -->
    <rect class="block-frame" x="340" y="75" width="160" height="45" />
    <!-- 虚线框：块3 只框下标12-13 -->
    <rect class="block-frame" x="500" y="75" width="80" height="45" />
    <!-- 复杂度标注 -->
    <text class="complexity" x="320" y="70">边界 O(B)</text>
    <text class="complexity" x="420" y="70">整块 O(1)</text>
    <text class="complexity" x="540" y="70">边界 O(B)</text>
    <!-- 查询区间线 -->
    <line class="query-line" x1="320" y1="45" x2="560" y2="45" />
    <text class="annotation" x="440" y="38">查询区间 [7, 13]</text>
    <!-- 底部大括号 -->
    <path class="bracket" d="M 40,130 L 40,135 L 160,135 L 160,130" />
    <path class="bracket" d="M 200,130 L 200,135 L 320,135 L 320,130" />
    <path class="bracket" d="M 360,130 L 360,135 L 480,135 L 480,130" />
    <path class="bracket" d="M 510,130 L 510,135 L 610,135 L 610,130" />
    <!-- 块编号 -->
    <text class="blk-label" x="100" y="150">块 0 (0‑3)</text>
    <text class="blk-label" x="260" y="150">块 1 (4‑7)</text>
    <text class="blk-label" x="420" y="150">块 2 (8‑11)</text>
    <text class="blk-label" x="560" y="150">块 3 (12‑14)</text>
</svg>

## 存储
- **全局信息**
  - 元素原始信息
  - 元素所属块
  - 块数与块长
- **块内信息**
  - 整块信息
  - 左右边界
  - 懒标记
> 以区间和为例
```cpp
struct Block{
    int sum = 0; // 块内和
    int l = 0, r = 0; // 块边界
    int add = 0; // 区间加懒标记
};
// 原始数组 和 元素所属块
vector<int> arr, belong;
// 块数组
vector<Block> block;
// 块数与块长
int num = 0, len = 0;
```

## 操作
### 预处理
- 计算块长与块数 *（全局级）*
- 标记块的边界 *（块级）*
- 标记元素所属块并计算块内和 *（元素级）*
```cpp
// 1-based 数组 a
void init(vector<int> &a){
    int n = a.size();
    arr = a;
    // 块长
    len = sqrt(n * 1.0);
    // 块数（有余数时多加一个不完整块）
    num = n / len + (n % len != 0);
    // 块数组（1-based）
    block.resize(num + 1);
    for(int i = 1;i <= num;i++)
        block[i].l = (i - 1) * len + 1,
        block[i].r = i * len;
    // 单独处理最后一个块
    block[num].r = n;
    // 块内和与归属标记
    for(int i = 1;i <= num;i++)
        for(int j = block[i].l;j <= block[i].r;j++)
            block[i].sum += arr[j], 
            belong[j] = i;
}
```

### 单点更新
直接更新该元素和所属块信息
```cpp
void add(int pos, int val){
    arr[pos] += val;
    block[belong[pos]].sum += val;
}
```

### 单点查询
直接查询该元素信息，并叠加所属块懒标记
```cpp
int query(int pos){
    return arr[pos] + block[belong[pos]].add;
}
```

### 区间更新
- **完全覆盖块**：打懒标记
- **部分覆盖块**
  - 逐项更新元素
  - 更新整块信息
```cpp
void add(int l, int r, int val){
    // 找到区间最左块和最右块
    int p = belong[l], q = belong[r];
    // 在一个块内，暴力更新
    if(p == q){
        for(int i = l;i <= r;i++) 
            arr[i] += val, block[p].sum += val;
        return;
    }
    // 处理完全覆盖块，打懒标记
    for(int i = p + 1;i <= q - 1;i++) 
        block[i].add += val;
    // 处理左右块，暴力更新
    for(int i = l;i <= block[p].r;i++)
       arr[i] += val, block[p].sum += val;
    for(int i = block[q].l;i <= r;i++)
      arr[i] += val, block[q].sum += val;
}
```

### 区间查询
- **完全覆盖块**
  - 逐块累加块内信息
  - 累加懒标记
- **部分覆盖块**
  - 逐项累加元素
  - 累加懒标记
```cpp
int query(int l, int r){
    int p = belong[l], q = belong[r], res = 0;
    // 在一个块内，暴力查询
    if(p == q){
        for(int i = l;i <= r;i++) res += arr[i];
        return res;
    }
    // 处理完全覆盖块，累加块内信息和懒标记
    for(int i = p + 1;i <= q - 1;i++) 
        res += block[i].sum,
        res += block[i].add * len;
    // 处理左右块，暴力查询
    for(int i = l;i <= block[p].r;i++) 
        res += arr[i] + block[p].add;
    for(int i = block[q].l;i <= r;i++) 
        res += arr[i] + block[q].add;
}
```

## 复杂度
- 单点更新：$O(1)$
- 单点查询：$O(1)$
- 区间更新：$O(\sqrt n)$
- 区间查询：$O(\sqrt n)$

